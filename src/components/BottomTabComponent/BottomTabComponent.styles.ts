import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  tabContainer: {
    flexDirection: 'row',
    height: '8%',
    justifyContent: 'space-around',
    alignItems: 'center',
  },
  buttonContainer: {
    justifyContent: 'center',
    alignItems: 'center',
    width: '25%',
  },
  badge: {position: 'absolute', top: -4, right: 20},
});
