import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    height: '25%',
  },
  component: {
    flexDirection: 'row',
    alignItems: 'center',
    marginHorizontal: '4%',
  },
});
